Vagrant Sandbox for playing with Docker
=======================================


**Provisioning environment**

.. code-block:: bash

   $ vagrant up
   $ vagrant ssh


It installs Ubuntu14.04 with Docker v1.5 and official images: `ubuntu`, `postgres`, `redis`, `mysql`.

